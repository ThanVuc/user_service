version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "sql/query"
    schema: "sql/schema"
    gen:
      go:
        package: "database"
        out: "./internal/grpc/database"
        sql_package: "pgx/v5"
